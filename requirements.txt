# requirements.txt

# --- 核心框架 ---
fastapi>=0.95.0      # 我们用来创建标准网络服务的“接待员”框架
mcp>=0.1.0           # 项目本身的核心协议框架

# --- 网络服务器 ---
# uvicorn 主要用于开发环境，但放在这里也没问题
uvicorn>=0.22.0
# gunicorn 是生产环境标准的、更健壮的网络服务器，Render 默认使用它
gunicorn

# --- 其他依赖 ---
requests>=2.28.0     # 项目用来发送网络请求的工具包 
